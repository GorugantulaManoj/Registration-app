apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "registration-chart.fullname" . }}-test-connection"
  labels:
    {{- include "registration-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "registration-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
